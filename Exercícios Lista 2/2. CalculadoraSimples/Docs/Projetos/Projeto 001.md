# Projeto v. 0.0.1

## Projeto de Interface com o Usuário

A interface será em terminal de linhas de comando

## Projeto de arquitetura

A aplicação terá dois módulos phyton, com a seguinte relação de nomes:

1. __main__
2. CalculadoraSimples

## Estruturas de dados e tipos

A classe CalculadoraSimples conterá os seguintes atributos e tipos:

1. last_result: float

A classe CalculadoraSimples terá os seguintes métodos com as respectivas assinaturas:

soma(self, num_1: float, num_2: float) -> float:

sub(self, num_1: float, num_2: float) -> float:

mult(self, num_1: float, num_2: float) -> float:

div(self, num_1: float, num_2: float) -> float:

last(self) -> float: